  <nav class="navbar navbar-expand-sm navbar-dark bg-primary sticky-top">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">CodeSoft</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link" aria-current="page" href="/">Search</a>
          </li>
          <li><%= link_to "Active Violations", violations_path, class:"nav-link" %></li>
          <li><%= link_to "Codes", codes_path, class:"nav-link" %></li>
          <% if user_signed_in? %>
          <li>
            <!-- <%= link_to "Logout", destroy_user_session_path, method: :delete, class:"nav-link", data: { turbo: false } %>-->
          </li>
          <% else %>
          <li>
            <%= link_to "Sign In", new_user_session_path, class:"nav-link", data: { turbo: false } %>
          </li>
          <li>
            <%= link_to "Sign Up", new_user_registration_path, class:"nav-link", data: { turbo: false } %>
          </li>
          <% end %>
        </ul>
      </div>
    </div>
  </nav>